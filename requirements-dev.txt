# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt
# This will install both development tools and all production dependencies from requirements.txt

-r requirements.txt

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
